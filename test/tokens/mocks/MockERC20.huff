
// !! WARNING !!
// This code is meant to be appended to src/tokens/ERC20.huff
// Thus, it does not import any other files

// Constructor
#define macro CONSTRUCTOR() {
    // Construct the ERC20
    CONSTRUCTOR_ERC20()

    // Mint the sender a supply
    caller   // [to]
    0x100    // [value, to]
    _MINT()  // []
}

// Main Entrypoint
#define macro MAIN() {
    MAIN_ERC20()
}
